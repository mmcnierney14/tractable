to pull someone else's branch:
git checkout -b name_of_branch
git pull origin name_of_branch

to create a new branch:
git pull origin name_of_branch

to update a branch:
git push origin name_of_branch

to check which branch you're on:
git status

to switch branches:
git checkout name_of_branch